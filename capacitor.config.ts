import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'capacitor-watchkit-example',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
